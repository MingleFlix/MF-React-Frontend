apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "react-frontend.fullname" . }}
  labels:
    {{- include "react-frontend.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "react-frontend.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "react-frontend.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          ports:
            - name: http
              containerPort: 3000
              protocol: TCP
